package com.capgemini.paas.skills_service.model.enums;

import com.capgemini.paas.skills_service.model.validation.ObjectValidator;

public enum Priority implements ObjectValidator {
    HIGH,
    MEDIUM,
    LOW
}